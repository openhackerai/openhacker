{
  "name": "@openhacker/cli",
  "version": "0.0.0",
  "description": "Openhacker CLI",
  "license": "UNLICENSED",
  "type": "module",
  "bin": {
    "openhacker": "./bin/openhacker"
  },
  "files": [
    "bin",
    "scripts",
    "src",
    "README.md"
  ],
  "scripts": {
    "dev": "bun run ./src/cli.ts",
    "build:binary": "bun run ./scripts/build-release.ts --binary-only",
    "build:release": "bun run ./scripts/build-release.ts"
  },
  "engines": {
    "bun": ">=1.3.0"
  },
  "dependencies": {
    "openhacker": "workspace:*"
  }
}
